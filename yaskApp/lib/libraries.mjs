/**
 * library structures and dependencies are being pulled and generated from cloudCache (firebase)
 * TODO: different webIDE's using
 * installation mode : webIDE, IDE, OSdependent
 */


import { http2 } from "http2";

const libraryModule (library, consolidate) => {
  let modules = [library.name, ...library.keys];
  let installationMode = 
}
